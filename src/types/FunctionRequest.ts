/**
 * Describes the input payload received by the Genesys Cloud function.
 */
export type FunctionRequest = {
  /** Example input field — replace with your own. */
  readonly exampleInput?: string;
};
